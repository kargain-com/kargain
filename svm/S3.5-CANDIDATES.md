# S3.5 candidates (from S3 stand)

Rows enter this list **only** when the local cross-VM stand (or a named host scenario) reproduces a defect that requires an EVM bytecode or parametric change at Nuclear #5. Empty is a valid resting state.

| ID | Defect (reproduced how) | Proposed EVM / model change | Status |
|----|-------------------------|-----------------------------|--------|
| _(none yet)_ | | | |

## Rules

- Do **not** promote plan-volume A items here (those are already known without S3).
- Do **not** invent CU/rent pins or speculative ABI changes.
- A row needs: failing scenario name, observed refusal/invariant break, and the minimal owner change.
