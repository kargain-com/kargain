[package]
name = "money-harness"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Local-validator harness for SPL payout reachability (S6 #1-fix) — not production"

[lib]
crate-type = ["cdylib", "lib"]
name = "money_harness"

[features]
no-entrypoint = []

[dependencies]
borsh = { workspace = true }
solana-program = { workspace = true }
kargain-errors = { workspace = true }
kargain-claimable-payouts = { workspace = true }
