[package]
name = "kargain-claimable-payouts"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "ClaimablePayouts — SPL attempt+claim; one PDA per (recipient, mint) (D-01/D-04)"

[dependencies]
kargain-errors = { workspace = true }
kargain-events = { workspace = true }
borsh = { workspace = true }
solana-program = { workspace = true }

[dev-dependencies]
