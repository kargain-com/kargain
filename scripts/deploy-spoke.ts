/**
 * @deprecated C2 — KarPassportONFT721 spoke deploy replaced by KarPassportBridgeGateway.
 * Dual-chain gateway deploy + pathway cutover ships in C2.
 *
 * Usage (aborts):
 *   pnpm deploy:spoke:sepolia
 */
console.error(
  "TODO(C2): KarPassportBridgeGateway spoke deploy — KarPassportONFT721 artifact removed.\n" +
    "Use the spoke gateway deploy path once C2 cutover lands; do not run this script.",
);
process.exit(1);
