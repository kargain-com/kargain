/**
 * @deprecated C2 — ProxyONFT721Adapter redeploy replaced by KarPassportBridgeGateway.
 * Additive gateway deploy + pathway cutover ships in C2.
 *
 * Usage (aborts):
 *   pnpm deploy:adapter:sepolia -- --force
 */
console.error(
  "TODO(C2): KarPassportBridgeGateway deploy — ProxyONFT721Adapter artifact removed.\n" +
    "Use the hub gateway deploy path once C2 cutover lands; do not run this script.",
);
process.exit(1);
