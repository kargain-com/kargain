import { createConfig } from "ponder";

// TODO Phase 1.1: Ponder config will be rebuilt after new
// contracts are deployed and ABIs are generated.
export default createConfig({
  networks: {},
  contracts: {},
});
