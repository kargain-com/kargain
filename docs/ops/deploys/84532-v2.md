# Base Sepolia generation v2 — deploy record (84532)

Point-in-time deploy log for the June 27, 2026 cutover. **Addresses and semver:** [contracts/SPEC.md Part I.9.1](../contracts/SPEC.md#i91-active-deployment-base-sepolia-84532).

| | |
|--|--|
| Date | June 27, 2026 |
| Git commit | `c88b5dc` — `feat(contracts): v2 deploy to Base Sepolia (84532)` |
| Deployer (public) | `0xcf1Eb0E7ed453Ed266bF90E7C09e0E4769580b77` |
| Manifest | `deployments/84532.json` — `generation: "v2"`, `indexFromBlock`: **43399242** (not in git; deploy machine only) |

---

## Post-deploy checks

| Check | Result |
|-------|--------|
| `pnpm smoke:sepolia` | **15/15 pass** |
| `pnpm verify:sepolia` | **Partial** — Timelock48h verified; others HHE80009 vs local compile. Smoke confirms on-chain behavior. |
| `pnpm hardhat test` | 238/238 (pre-deploy) |

---

## VPS cutover — complete (June 2026)

| Step | Result |
|------|--------|
| `git pull` on VPS | ✅ `SEPOLIA_ACTIVE` in `lib/web3/sepolia-addresses.ts` |
| `pnpm ponder:config` | ✅ `source: committed`, `karPassport: 0x2C46…`, `indexFromBlock: 43399242` |
| `docker compose build ponder` | ✅ (requires `COPY patches` in Dockerfile.ponder) |
| `./scripts/ponder-reindex.sh` + recreate ponder | ✅ Backfill from **43399242** |
| API smoke | ✅ v1 ghost `passport id 0` gone; indexer on v2 contract set |

**Steady VPS `.env`:**

```bash
PONDER_RPC_URL_84532=https://sepolia.base.org
PONDER_START_BLOCK_84532=43399242
```

No `PONDER_*_ADDRESS` overrides needed after git pull. Vercel: no `NEXT_PUBLIC_*` contract overrides — committed fallbacks in [`lib/web3/sepolia-addresses.ts`](../../lib/web3/sepolia-addresses.ts).

Runbook: [indexer/OPERATIONS.md](../indexer/OPERATIONS.md)

---

## What works / remaining gaps

**Works:** Generation v2 on-chain; Timelock48h governance; USDC + USD registry; production Ponder on v2 addresses from block 43399242.

**Remaining:** Frontend v2 UI; LZ peers; Basescan verify gap; deferred marketplace events ([indexer/MIGRATION-V2.md](../indexer/MIGRATION-V2.md) phase 2).

Runbook after deploy: rebuild ponder image + [ponder-reindex.sh](../../scripts/ponder-reindex.sh).

---

## Troubleshooting

| Symptom | Action |
|---------|--------|
| `passport id 0` / v1 Test data in API | Stale DB or old Docker image — rebuild ponder, run [ponder-reindex.sh](../../scripts/ponder-reindex.sh) |
| Empty browse | Expected until v2 mints/listings on-chain; confirm `/health` and sync logs |
| Docker build ENOENT patches | Pull latest `Dockerfile.ponder` (`COPY patches` before install) |
| `verify:sepolia` mismatch | Verify from deploy artifacts; addresses in SPEC I.9.1 still valid |
| RPC timeout on deploy | Retry `pnpm deploy:sepolia`; partial progress is in `deployments/84532.json` |

---

## Deploy notes

- RPC flakiness during deploy — retry `pnpm deploy:sepolia` (manifest records completed steps)
- KarProPass reused — no on-chain `VERSION()`
- Canonical deploy blocks ~43399252–43399505
