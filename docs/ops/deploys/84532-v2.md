# Base Sepolia generation v2 — deploy record (84532)

Point-in-time deploy log for the June 27, 2026 cutover. **Addresses and semver:** [contracts/SPEC.md Part I.9.1](../contracts/SPEC.md#i91-active-deployment-base-sepolia-84532).

| | |
|--|--|
| Date | June 27, 2026 |
| Git commit | `c88b5dc` — `feat(contracts): v2 deploy to Base Sepolia (84532)` |
| Deployer (public) | `0xcf1Eb0E7ed453Ed266bF90E7C09e0E4769580b77` |
| Manifest | `deployments/84532.json` — `generation: "v2"`, `indexFromBlock`: **43399242** (not committed; copy to VPS after deploy) |

---

## Post-deploy checks

| Check | Result |
|-------|--------|
| `pnpm smoke:v2` | **15/15 pass** |
| `pnpm verify:v2` | **Partial** — Timelock48h verified; others HHE80009 vs local compile. Smoke confirms on-chain behavior. |
| `pnpm hardhat test` | 238/238 (pre-deploy) |

---

## What works / gaps

**Works:** Generation v2 on-chain; Timelock48h governance; USDC + USD registry; `pnpm smoke:v2`.

**Gaps:** Ponder cutover ([indexer/MIGRATION-V2.md](../indexer/MIGRATION-V2.md)); frontend v2 UI; LZ peers; Basescan verify gap.

---

## VPS cutover checklist

1. Copy `deployments/84532.json` to VPS
2. `node --import tsx scripts/lib/print-ponder-env.ts` → set `PONDER_*` and `PONDER_START_BLOCK_84532=43399242`
3. Ponder schema/handlers — [indexer/MIGRATION-V2.md](../indexer/MIGRATION-V2.md)
4. Reindex — [indexer/OPERATIONS.md](../indexer/OPERATIONS.md)
5. Optional Vercel `NEXT_PUBLIC_*` from print-ponder-env or committed fallbacks in `lib/web3/deployment-addresses.ts`

---

## Troubleshooting

| Symptom | Action |
|---------|--------|
| Empty browse | Ponder migration + reindex from 43399242 |
| `verify:v2` mismatch | Verify from deploy artifacts; addresses in SPEC I.9.1 still valid |
| RPC timeout on deploy | Retry `pnpm deploy:v2`; partial progress is in `deployments/84532.json` |
| Orphan timelocks | Ignore; use SPEC I.9.1 addresses |

---

## Deploy notes

- RPC flakiness during deploy — retry `pnpm deploy:v2` (manifest records completed steps)
- KarProPass reused — no on-chain `VERSION()`
- Canonical deploy blocks ~43399252–43399505
