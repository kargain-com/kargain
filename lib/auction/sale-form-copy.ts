/** Seller-facing hint when a fixed-price listing blocks auction create (custody mutex). */
export const DELIST_BEFORE_AUCTION_HINT =
  "To start a reserve auction, delist this fixed-price listing first.";

/** Owner-facing hint when passport verification blocks reserve auctions. */
export const AUCTION_REQUIRES_VERIFICATION_HINT =
  "Reserve auctions require a verified passport. Get this vehicle verified by a KarPro to enable auctions.";
